export default function ProjectLoading() {
  return (
    <div style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "var(--bg-0)", color: "var(--fg-1)", fontFamily: "var(--font-mono)" }}>
      Loading project workspace…
    </div>
  );
}
